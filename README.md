# Teardown Lab thumbnails

Public hero thumbnails for the Teardown Lab catalog (Notion). Small JPGs used as a
transient source so the Notion connector can pull them in via `source_url` — Notion
stores its own copy after attaching. Filenames = project slug.
